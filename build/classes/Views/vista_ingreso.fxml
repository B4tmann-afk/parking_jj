<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns="http://javafx.com/javafx/21"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="Controller.IngresoController"
            prefWidth="600" prefHeight="500">
    
    <!-- ENCABEZADO -->
    <top>
        <VBox style="-fx-background-color: #27ae60; -fx-padding: 20px;">
            <Label text="🚗 Registrar Ingreso al Parqueadero" textFill="white">
                <font>
                    <Font name="System Bold" size="22"/>
                </font>
            </Label>
            <Label text="Complete los datos del vehículo que ingresa" 
                   textFill="#ecf0f1" style="-fx-font-size: 13px;">
                <VBox.margin>
                    <Insets top="5"/>
                </VBox.margin>
            </Label>
        </VBox>
    </top>
    
    <!-- CONTENIDO PRINCIPAL -->
    <center>
        <VBox spacing="25" style="-fx-padding: 30px;">
            
            <!-- FORMULARIO -->
            <VBox spacing="18" style="-fx-background-color: white; -fx-padding: 25px; -fx-background-radius: 10px; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.15), 12, 0, 0, 3);">
                <Label text="📋 Información del Vehículo" style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
                
                <GridPane hgap="20" vgap="15">
                    <!-- Fila 1: Placa (obligatorio) -->
                    <Label text="Placa: *" GridPane.columnIndex="0" GridPane.rowIndex="0"
                           style="-fx-font-weight: bold; -fx-text-fill: #e74c3c;"/>
                    <TextField fx:id="txtPlaca" 
                               promptText="Ej: ABC123" 
                               GridPane.columnIndex="1" GridPane.rowIndex="0"
                               prefWidth="300"
                               style="-fx-font-size: 14px; -fx-padding: 8px;"/>
                    
                    <!-- Fila 2: Tipo (obligatorio) -->
                    <Label text="Tipo: *" GridPane.columnIndex="0" GridPane.rowIndex="1"
                           style="-fx-font-weight: bold; -fx-text-fill: #e74c3c;"/>
                    <ComboBox fx:id="cmbTipo" 
                              GridPane.columnIndex="1" GridPane.rowIndex="1"
                              prefWidth="300"
                              style="-fx-font-size: 14px;"/>
                    
                    <!-- Fila 3: Marca (opcional) -->
                    <Label text="Marca:" GridPane.columnIndex="0" GridPane.rowIndex="2"
                           style="-fx-font-weight: bold;"/>
                    <TextField fx:id="txtMarca" 
                               promptText="Ej: Toyota, Honda, Yamaha" 
                               GridPane.columnIndex="1" GridPane.rowIndex="2"
                               prefWidth="300"
                               style="-fx-font-size: 14px; -fx-padding: 8px;"/>
                    
                    <!-- Fila 4: Modelo (opcional) -->
                    <Label text="Modelo:" GridPane.columnIndex="0" GridPane.rowIndex="3"
                           style="-fx-font-weight: bold;"/>
                    <TextField fx:id="txtModelo" 
                               promptText="Ej: Corolla 2020, CBR 600" 
                               GridPane.columnIndex="1" GridPane.rowIndex="3"
                               prefWidth="300"
                               style="-fx-font-size: 14px; -fx-padding: 8px;"/>
                    
                    <!-- Fila 5: Cliente (opcional) -->
                    <Label text="Cliente:" GridPane.columnIndex="0" GridPane.rowIndex="4"
                           style="-fx-font-weight: bold;"/>
                    <TextField fx:id="txtCliente" 
                               promptText="Nombre del cliente (opcional)" 
                               GridPane.columnIndex="1" GridPane.rowIndex="4"
                               prefWidth="300"
                               style="-fx-font-size: 14px; -fx-padding: 8px;"/>
                </GridPane>
                
                <Separator/>
                
                <!-- Nota informativa -->
                <HBox spacing="8" alignment="CENTER_LEFT">
                    <Label text="ℹ️" style="-fx-font-size: 16px;"/>
                    <Label text="Los campos marcados con * son obligatorios. Si es un vehículo nuevo, se registrará automáticamente."
                           style="-fx-font-size: 11px; -fx-text-fill: #7f8c8d; -fx-wrap-text: true;"
                           maxWidth="450"/>
                </HBox>
            </VBox>
            
            <!-- BOTONES -->
            <HBox spacing="15" alignment="CENTER">
                <Button text="✅ Registrar Ingreso" 
                        onAction="#registrarIngreso"
                        defaultButton="true"
                        style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-font-size: 15px; -fx-font-weight: bold; -fx-padding: 12px 30px; -fx-cursor: hand; -fx-background-radius: 5px;"/>
                
                <Button text="❌ Cancelar" 
                        onAction="#cerrarVentana"
                        cancelButton="true"
                        style="-fx-background-color: #95a5a6; -fx-text-fill: white; -fx-font-size: 15px; -fx-font-weight: bold; -fx-padding: 12px 30px; -fx-cursor: hand; -fx-background-radius: 5px;"/>
            </HBox>
        </VBox>
    </center>
</BorderPane>