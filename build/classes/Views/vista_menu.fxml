<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns="http://javafx.com/javafx/21"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="Controller.MenuController"
            prefWidth="900" prefHeight="700">
    
    <!-- ENCABEZADO -->
    <top>
        <VBox style="-fx-background-color: linear-gradient(to bottom, #2c3e50, #34495e); -fx-padding: 25px;">
            <HBox alignment="CENTER_LEFT" spacing="20">
                <VBox HBox.hgrow="ALWAYS">
                    <Label text="🅿️ Sistema de Parqueadero" textFill="white">
                        <font>
                            <Font name="System Bold" size="28"/>
                        </font>
                    </Label>
                    <Label text="Gestión integral de estacionamiento" 
                           textFill="#ecf0f1" style="-fx-font-size: 14px;">
                        <VBox.margin>
                            <Insets top="5"/>
                        </VBox.margin>
                    </Label>
                </VBox>
                
                <!-- Usuario actual -->
                <Label fx:id="lblUsuario" text="👤 Usuario" textFill="white"
                       style="-fx-font-size: 14px; -fx-font-weight: bold; -fx-background-color: rgba(255,255,255,0.2); -fx-padding: 10px 15px; -fx-background-radius: 5px;"/>
            </HBox>
            
            <!-- Tarifas actuales -->
            <HBox spacing="30" style="-fx-padding: 15px 0 5px 0;" alignment="CENTER_LEFT">
                <Label text="📊 Tarifas Actuales:" textFill="#ecf0f1" style="-fx-font-weight: bold; -fx-font-size: 13px;"/>
                <Label fx:id="lblTarifaMoto" text="🏍️ Moto: $-" textFill="white" style="-fx-font-size: 13px;"/>
                <Label fx:id="lblTarifaCarro" text="🚗 Carro: $-" textFill="white" style="-fx-font-size: 13px;"/>
            </HBox>
        </VBox>
    </top>
    
    <!-- CONTENIDO PRINCIPAL -->
    <center>
        <VBox spacing="25" style="-fx-padding: 30px; -fx-background-color: #ecf0f1;">
            
            <!-- SECCIÓN: OPERACIONES -->
            <VBox spacing="15">
                <Label text="📋 OPERACIONES DIARIAS" 
                       style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
                
                <GridPane hgap="15" vgap="15">
                    <!-- Ingreso -->
                    <Button text="➕ Registrar Ingreso" 
                            onAction="#abrirIngreso"
                            GridPane.columnIndex="0" GridPane.rowIndex="0"
                            prefWidth="280" prefHeight="80"
                            style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold; -fx-background-radius: 10px; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);"/>
                    
                    <!-- Salida -->
                    <Button text="➖ Registrar Salida" 
                            onAction="#abrirSalida"
                            GridPane.columnIndex="1" GridPane.rowIndex="0"
                            prefWidth="280" prefHeight="80"
                            style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold; -fx-background-radius: 10px; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);"/>
                    
                    <!-- Vehículos -->
                    <Button text="🚗 Gestionar Vehículos" 
                            onAction="#abrirVehiculos"
                            GridPane.columnIndex="2" GridPane.rowIndex="0"
                            prefWidth="280" prefHeight="80"
                            style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold; -fx-background-radius: 10px; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);"/>
                </GridPane>
            </VBox>
            
            <Separator/>
            
            <!-- SECCIÓN: ADMINISTRACIÓN (Solo Admin) -->
            <VBox spacing="15">
                <Label text="⚙️ ADMINISTRACIÓN" 
                       style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
                
                <GridPane hgap="15" vgap="15">
                    <!-- Tarifas -->
                    <Button fx:id="btnTarifas"
                            text="💰 Gestionar Tarifas" 
                            onAction="#abrirTarifas"
                            GridPane.columnIndex="0" GridPane.rowIndex="0"
                            prefWidth="280" prefHeight="80"
                            style="-fx-background-color: #f39c12; -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold; -fx-background-radius: 10px; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);"/>
                    
                    <!-- Clientes -->
                    <Button fx:id="btnClientes"
                            text="👥 Gestionar Clientes" 
                            onAction="#abrirClientes"
                            GridPane.columnIndex="1" GridPane.rowIndex="0"
                            prefWidth="280" prefHeight="80"
                            style="-fx-background-color: #9b59b6; -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold; -fx-background-radius: 10px; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);"/>
                    
                    <!-- Trabajadores -->
                    <Button fx:id="btnTrabajadores"
                            text="👨‍💼 Gestionar Trabajadores" 
                            onAction="#abrirTrabajadores"
                            GridPane.columnIndex="2" GridPane.rowIndex="0"
                            prefWidth="280" prefHeight="80"
                            style="-fx-background-color: #16a085; -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold; -fx-background-radius: 10px; -fx-cursor: hand; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 8, 0, 0, 2);"/>
                </GridPane>
            </VBox>
            
            <Separator/>
            
            <!-- Botón Salir -->
            <HBox alignment="CENTER">
                <Button text="🚪 Salir del Sistema" 
                        onAction="#salir"
                        prefWidth="250" prefHeight="50"
                        style="-fx-background-color: #7f8c8d; -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold; -fx-background-radius: 8px; -fx-cursor: hand;"/>
            </HBox>
        </VBox>
    </center>
</BorderPane>